# Cloudflare Pages configuration
# Auth and API logic handled by Cloud Run backend — no KV or env vars needed.
# The single functions/api/[[path]].ts proxy forwards /api/* to Cloud Run.

name = "dc-dev-hub"
pages_build_output_dir = "dist"
compatibility_date = "2024-01-01"
